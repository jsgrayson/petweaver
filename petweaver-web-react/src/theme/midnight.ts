import { Theme } from './types';
import { colors, gradients } from './tokens';
import { rarity, glows } from './glow';
import { backgrounds } from './backgrounds';
import { layers } from './layers';

export const Midnight: Theme = {
  colors,
  gradients,
  rarity,
  glows,
  backgrounds,
  layers,
};
